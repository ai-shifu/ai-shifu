# Tasks

Design: [Primary surface rules completion](./design.md)

- [x] Add hand-maintained local `AGENTS.md` files for `.github`, `docker`, and
      `scripts`.
- [x] Expand `docs/engineering-baseline.md` with repo-specific CI/CD and
      release workflow guidance.
- [x] Extend the AI-doc generator with new `CLAUDE.md`, Cursor, and Copilot
      mirrors for the new primary surfaces.
- [x] Extend AI-doc validation so the new manual rule entry points and
      baseline heading are required.
- [x] Regenerate mirrors, run validation plus focused pre-commit checks, and
      leave the checklist current.
